name: "Release Tests & Analysis"

on:
  pull_request:
    branches: 
      - 'release/**'
    paths:
      - 'src/FinancialHub/*' 
      - 'src/FinancialHub/**'

permissions:
  checks: write
  pull-requests: write

jobs: 
  tests:
    uses: ./.github/workflows/tests_workflow.yml
    with:
      dir: ./src/FinancialHub/
    secrets: inherit